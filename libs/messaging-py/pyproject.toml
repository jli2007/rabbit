[project]
name = "messaging"
version = "0.1.0"
description = "Add your description here"
readme = "readme.md"
requires-python = ">=3.13"
dependencies = [
    "aio-pika>=9.6.1",
    "dotenv>=0.9.9",
]
